# cov/android-driver: pin the Robolectric SDK image so the JVM driver suite runs against a single,
# widely-cached android-all jar (SDK 33) rather than whatever the compileSdk happens to be. minSdk is 29
# and none of the exercised paths need a newer framework.
sdk=33
